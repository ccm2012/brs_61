<% provide :title, t("user_for.all_users") %>
<h1><%= t "user_for.all_users" %></h1>

<% if can? :create, User %>
  <%= link_to t("pages.create_user"), new_user_path,
    class: "btn btn-default btn-lg active pull-right" %>
<% end %>
<%= will_paginate %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate %>
