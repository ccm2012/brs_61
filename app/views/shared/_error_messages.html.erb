<% if user.errors.any? %>
  <div id="error-explanation">
    <div class="alert alert-danger">
      <%= t "pages.form_contain" %> <%= pluralize user.errors.count,
        t("pages.errors") %>.
    </div>
    <ul>
      <% user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
